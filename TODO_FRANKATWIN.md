# Joint-space 仿真参数辨识与真机采集 TODO

更新日期：2026-09-23。本文记录待办与验收条件，不表示功能已完成或已通过真机验证。

评估基线：`feat/sysid-1khz-ring-log`，提交
`2c960fb526d3aca0ddad7c80275cfee4b5c1e08b`，对应
[PR #5](https://github.com/Robot-Dexterity-Lab/fr3_stack/pull/5)。
上游参考：[kingchou007/frankatwin，v0.3](https://github.com/kingchou007/frankatwin/tree/v0.3)，
本次阅读的提交为 `3c7b43ee95cc4631915191928dd2648dba8235c6`。

修复状态：`feat/sysid-1khz-ring-log` 的独立工作区 `../fr3_stack-sysid-fix`
已实现 writer 错误处理、CSV v2 joint 日志，以及独立的录制开始／停止／状态接口，
目前为本地未提交改动。使用说明见该工作区 `docs/recording.md`。
本文件所在 main 工作区保留既有 SI 脚本和文档；完整 daemon 构建与真机验收仍未完成。

目标：完善现有 fr3-stack 日志，在真机采集可复现数据；独立 sim 回放相同关节目标，
通过闭环轨迹误差拟合仿真参数，完成独立验证后再评估训练与部署效果。
这里采用的是仿真参数辨识，不以传统逆动力学回归作为主路线。

已确定采用 joint 路线：采集脚本或策略生成关节位置目标，真机使用关节阻抗，
独立 sim 根据文件中的控制契约回放。SimToolReal 不依赖或导入 fr3-stack。
用户确认安装顺序为 FR3 → 连接件 → XHand，安装与坐标系和 sim 一致；
现场质量、重心、惯量仍待确认。资产 URDF 中 XHand 质量合计约 1.101 kg，
连接件没有独立惯性定义，该模型值不能直接作为整套实机末端的负载。

## 仓库职责与当前状态

| 仓库／执行方 | 已存在 | 本轮路线中的职责与缺口 |
| --- | --- | --- |
| `fr3_stack` | 连续 joint 接口、NUC 1 kHz 阻抗、新增激励脚本；功能分支工作区已修 writer、补 joint 日志 | 完整构建、NUC 验收、实验元数据和导出仍待完成；不需要重新造 joint 接口 |
| `kingchou007/frankatwin` | 所审提交的 SI 为 Cartesian task impedance；`move_to_q()` 是阻塞关节复位 | 参考回放、优化与验证方法；原有入口不是连续 joint-space SI |
| `simtoolreal-fr3-xhand` | `franka_xhand_calibrated`、现有 implicit PD 训练路径 | 独立实现文件校验、joint 控制器回放、参数拟合、验证和 profile；尚未实现这条 SI 流程 |
| 现场操作人员 | 用户确认安装链和几何／坐标一致 | 确认负载、手型、实际控制配置和运动空间，分别验收两台机械臂 |

sim 仓库的完整设计在 `docs/joint_space_sysid.md`，实现清单在
`docs/frankatwin_sim_todo.md`。本文件负责真机侧和跨仓库验收顺序，
文件契约由 sim 设计统一描述，适配器不能引入兄弟仓库路径或运行时依赖。
当前更新的是本地设计、使用说明和 TODO；不是已完成全流程辨识的声明。

## 已完成的评估

- [x] 确认功能分支已有 1 kHz SPSC 环形缓冲区、非实时 CSV writer 和 daemon 的 `--log-1khz` 参数。
- [x] 日志模块独立编译与测试：45 项通过。
- [x] 控制器数学测试使用 libfranka mock 独立编译：74 项通过。
- [x] 复现写盘失败误报：向 `/dev/full` 写入后仍报告 `ok=1, written=1, dropped=0`。
- [x] 复现目标语义差异：10 mm 输入、EMA alpha=0.05 时，首周期弹簧目标为 0.5 mm，日志目标仍为 10 mm。
- [x] 尝试 CMake 配置：本机缺少 libfranka，配置受阻；完整 daemon 编译结果仍未知。
- [x] 新增 joint 轨迹脚本的软件验证：40 项轨迹/故障/FakeDaemon 测试通过，
  既有客户端 29 项测试通过；四种模式均完成离线预览，文档严格构建通过。
- [x] 本次日志修复：ring/writer 105 项断言、控制器与同周期采集 88 项断言通过；
  writer 的 AddressSanitizer / UndefinedBehaviorSanitizer 检查通过。
  覆盖短写、header/batch/final flush、close、`/dev/full`、80 列 CSV、目标变化／保持／重置／模式切换。
- [x] 独立录制接口的软件验证：包含实际 C++ REQ/REP 服务的完整 Python 回归 238 项通过；
  动态录制生命周期 24 项断言及 ASan/UBSan 通过，ring/writer 105 项回归通过。
  覆盖并发 stop 排空、连续录制、旧 ID、文件保护、I/O 错误、CLI、超时恢复和运动通道独立性。
  文档严格构建、launcher shell 语法和 Compose YAML 字段检查通过；本机无 Docker Compose CLI，
  完整 Compose 解析与容器运行待 NUC 验收。

以上是软件评估证据，不证明 NUC 实时性、完整构建或机器人行为已合格。
日志实现位于功能分支，当前保存本文的分支不一定包含这些源码。

## 1. 合并前修复与明确接口

- [x] 独立录制开关：Python `start_recording` / `stop_recording` / `recording_status`
  和 `fr3-recording` CLI，经独立 5557 REQ/REP 通道确认，不改变运动控制模式。
  默认关闭，stop 等待排空和关闭后返回；录制 ID 防止过期 stop 误停新实验，已有文件拒绝覆盖。
  Compose 增加 `/recordings` 持久挂载，可在同一 daemon 连续录多份文件。
- [ ] 在 NUC 更新并构建功能分支，工作站安装对应 Python 客户端；验收 5557 开关、
  控制和状态流不中断、stop 的 `complete` 与计数、宿主机持久文件和逐次录制时间／序号重置。
  旧 daemon 不支持该接口；main 工作区目前只有文档，不能只更新工作站就使用新开关。

- [x] **P1：修复写盘失败误报（本地功能分支，尚待部署）。**
  涉及 `src/ring_log_writer.cpp`、`include/fr3_stack/ring_log_writer.hpp`。
  检查 `fwrite`、`fflush`、`fclose` 的结果，保留可诊断的错误信息并使日志状态失效；
  明确计数代表入队、写入还是成功结束，不能把失败的日志报告为完整成功。
  文件处理和错误报告保持在非实时路径，不能因写盘失败在 RT 回调中阻塞或突然改变控制行为。
  **验收：** `/dev/full` 和模拟短写／刷新失败均可被检测；正常 CSV、退出排空与重复 stop 测试仍通过。
  已保留首个失败操作及 errno，区分 flushed/discarded/dropped；仅成功关闭且无丢帧才报告 I/O complete。
  失败不改变控制行为；daemon 正常结束所有线程后，以退出码 2 报告日志不完整。

- [ ] **P2：明确记录目标的处理阶段。**
  涉及 `Controller::pose_target()`、笛卡尔控制器和 `src/main.cpp`。
  当前记录的是调度器处理后的 `cfg_.target`：可以已经过插值，但仍在控制器 EMA 之前。
  选择并记录方案：保留该目标且在回放中复现 EMA 配置与初始状态，或额外记录滤波后目标。
  避免对已经插值的每周期目标再次施加客户端插值。
  **验收：** 对同一状态和目标序列，回放逐周期匹配控制器的弹簧项；覆盖 EMA 开／关、重置、流式目标和生成器路径。

- [x] 补充 writer 错误路径和 joint 目标语义的回归测试，运行日志测试和控制器数学测试。
  笛卡尔目标的完整回放语义仍按上面的 P2 跟进。
- [x] 为 joint 采集补充每周期关节目标。CSV v2 已记录控制器收到的目标和滤波后实际使用的目标，保存 K、D、
  `filter_alpha`、重置状态及补偿设置；明确采样与力矩计算的先后关系。
  **验收：** 已知关节目标序列可以重现逐周期滤波目标与控制输出。
  新增 `joint_target_valid`、reset counter、`robot_time_s` 和 `control_period_s`；
  原 45 列保持位置，追加 35 列。其他模式的 joint 字段明确无效，不能把零当成目标。
- [ ] 将 joint 目标记录与 writer 修复列为主路线的前置条件；笛卡尔目标语义修复
  保留为相应控制模式的独立事项，不能只修笛卡尔列就宣称 joint 数据完整。
- [ ] 为每个 tick 明确状态获取、命令选择、EMA、控制计算和最终限幅的顺序。
  当前 `t_s` 在回调末尾采集，不能直接标成状态采样时刻；记录实际周期和时间事件。
  初始滤波状态、限速器前一力矩、重置／切换和启动区间必须能复现。
  **验收：** 包含目标变化与保持的合成序列可逐帧关联状态、目标与输出，无一帧偏移。
  本地共享采集函数和同周期目标测试已通过，新增机器人时钟与回调周期；
  完整 dispatcher／NUC 验收及初始限速器状态、run 元数据仍需补充。
- [ ] 更新对应说明、`HARDWARE_CHECKLIST.md` 和 `CHANGELOG.md`；明确通过的检查与待验证项。
- [ ] 在具备匹配版本 libfranka 及其余依赖的构建环境中编译完整 daemon，验证 CMake 的两个测试目标。
  **验收：** 完整编译通过，`controller_math` 和 `ring_log` 均通过；保存构建版本与结果。

## 2. 真机采集入口与数据契约

- [ ] 为 `fr3-stack` 启动脚本和 `containers/compose.yml` 接入日志参数及持久化输出目录。
  当前参数属于 daemon，不能直接当成 `./fr3-stack up cart` 的参数使用。
  **验收：** 容器内日志写到明确的宿主机目录，启动时可见日志状态，重启不会意外覆盖已有实验数据。

- [x] 添加 `examples/joint_sysid.py`：hold / sine / chirp / multisine、离线 CSV/SVG
  预览，以及显式 `--execute` 的 `Robot.send_joint_impedance` 发送模式。
  使用当前静止关节姿态作为锚点，不自动回零；保存发送参数、时间、状态及失败原因。
  详见 [Joint excitation](docs/joint-sysid.md)。客户端记录明确标为非 1 kHz 数据。
- [ ] 在现场选定每关节幅度与频带并完成硬件验收。脚本检查关节范围与连续参考的
  速度、加速度和 jerk；尚无碰撞/力矩预测，手臂与手部扫掠空间仍需单独检查。
  不直接移植 FrankaTwin 笛卡尔示例参数，正式辨识仍依赖第 1 节的日志修复。

- [ ] 为每次采集生成带版本号的 JSON 元数据，并明确截取的实验时间区间。
  保存代码提交、机器人与固件版本、初始关节姿态、实际增益、目标保持／滤波配置、
  摩擦补偿开关、力矩限制、末端坐标系、工具负载、轨迹参数及异常结束原因。
  读取实际生效配置，不能只记录默认 YAML；注意 Python sticky cache。

- [ ] 实现日志完整性检查及面向独立 sim 的文件导出适配。
  对齐 sim 设计草案 `joint_sysid_recording/v1`，先冻结字段／类型再实现。
  当前 CSV 的 `q0..q6`、`ee_*`、`tgt_*` 与上游字段不同；显式映射关节名称、索引、单位和 xyzw 四元数。
  保留测量力矩与命令力矩的区别，以及采样／命令周期的时序语义。
  保留并检查 `t_s` 与 `seq`，不能盲目把缺帧日志当作均匀 1 kHz 数据。
  **验收：** 已知数值夹具转换正确；缺帧、写盘失败、控制器切换或异常终止的数据被拒绝或明确标为无效。
- [ ] 原始 NUC 数据只读保留，导出时记录原始文件哈希、转换器版本、过滤／拒绝原因、
  writer 完成状态及计数、配置来源、手型、负载和训练／验证划分。
  客户端 `fr3_joint_excitation/v1` 文件保持诊断用途，不能直接重命名为完整 1 kHz 数据。
- [ ] 以 NUC 实际生效目标的时间线作为回放输入，保留命令关联信息（若已支持）。
  PC 发送与 NUC 应用不属于同一个时钟；不能把时钟偏移、网络时间和滤波延迟全拟合成电机延迟。
  没有逐周期目标的数据不能用零填充或上游 Cartesian 文件替代。

## 3. NUC 与真机验收

此阶段需要现场操作人员执行。参考功能分支中的
[HARDWARE_CHECKLIST.md](https://github.com/Robot-Dexterity-Lab/fr3_stack/blob/2c960fb/HARDWARE_CHECKLIST.md)。

- [ ] 记录实际机器人型号、Desk 系统版本、NUC 实时内核和 libfranka 版本，确认版本匹配。
- [ ] 确认末端实际安装组合及工具坐标系、质量、重心、惯量；采集和仿真使用同一配置。
  若安装 XHand、Bota 或快换，不能直接沿用上游 Franka Hand 模型。
- [ ] 分别记录物理负载的测量／估算值与不确定性、Desk/libfranka 的配置值及其来源。
  标明重心坐标系、惯量参考点与包含的部件；`m_load=0` 不等于没有 XHand。
  负载未知时可以先开发离线校验和回放；名义负载拟合只能标为探索结果，不能验收为真实物理参数。
- [ ] 确认只由一个控制栈占用该机器人的 FCI；检查初始姿态及到达路径、运动空间和现场停止方式。
  将现场使用的误差／碰撞／力矩限制记录进实验配置，不能为跑完激励而任意放宽。
- [ ] 首先保持当前姿态记录约 60 秒：检查记录数量、`seq` 连续性、`t_s` 间隔分布及零丢帧。
  序号连续只说明已执行回调的记录连续，还要核对实际时序和 libfranka 通信／控制异常。
- [ ] 在相同条件下比较启用／禁用日志的行为和实时表现，记录异常、延迟与日志吞吐。
- [ ] 使用现场选定的小幅低频运动检查状态、目标、力矩列及结束行为，再逐步扩展激励范围。
- [ ] 在采集端确认 CSV 和元数据均保存成功后，再开始下一次实验。

**本阶段通过条件：** 完整构建和日志功能通过；保持与小幅运动均无未解释异常；
日志完整、目标语义明确、实时表现符合事先记录的验收阈值。

## 4. 仿真对齐、辨识与独立验证

- [ ] 在 sim 独立 calibration 环境采用名义 1 ms 物理／控制步长，固定 XHand 手型。
  显式 effort 控制时关闭冲突的 arm implicit drives；手部保持控制独立处理。
  只在起始时初始化 q/dq；每步的真实 q 是误差比较值，不能直接写入 sim 伪造回放一致。
- [ ] 在独立 IsaacLab 回放环境中复现记录的 joint 控制契约：关节目标滤波、K/D、
  关节限位作用、力矩限制，以及重力和 Coriolis 补偿的处理约定。
  文件交换不引入 fr3-stack 运行时依赖。若另做笛卡尔实验，再分别验证姿态误差、
  零空间与末端/Jacobian 参考点；不能仅复制 K、D 就认定两种控制实现等价。
- [ ] 先用确定性状态／目标序列比较控制输出，再比较整段轨迹。
  **验收：** 给定相同输入，控制输出在明确的数值容差内匹配，剩余差异有记录。
- [ ] 明确当前控制律为 `K*(EMA(q_ref)-q)-D*dq+Coriolis+限位项+可选摩擦补偿`，
  不是 `D*(dq_ref-dq)`。先回放 EMA 前目标并检查 EMA 后目标；诊断性回放 EMA 后目标时禁用重复滤波。
  重力和 libfranka 下游滤波／力矩约定另行核对，不能重复补偿或默认命令力矩等于实际总力矩。
- [ ] 按下面的 joint 轨迹方案采集辨识集与独立验证集。各组建议重复三次，
  记录温度/运行状态；同一组从相同已检查姿态开始，验证集不参与参数或轨迹调优。
- [ ] 第一轮确认并记录 `use_friction=false`；若后续部署启用补偿，应在仿真中复现并重新验证。
- [ ] 先核对 pinned IsaacLab/PhysX 的参数 API、单位、逐关节映射，写入后读回并做行为夹具。
  关节摩擦与接触摩擦不能混用；armature 不能重复计算 link 惯量。
- [ ] 从数据能约束的小参数集开始，逐步评估静／动／黏性摩擦、armature、执行延迟。
  记录有限物理边界及依据、固定参数、单位、delay 插入位置和以秒为单位的时间值。
  不要求直接套用上游 29 维；弱可辨识项固定为有来源的名义值。
- [ ] 固定目标函数：各训练 run 等权，q/dq 误差用训练数据或任务容差确定的固定尺度归一化。
  明确权重、评分区间与 warm-up；另报各关节 rad、rad/s 原始误差。
  不用验证集调归一化，不让每个候选自行缩放误差；发散结果保存原因并按明确失败规则处理。
- [ ] 先保存未辨识 baseline，运行小规模 seeded CMA-ES 验证，再依据实测耗时扩大搜索。
  保存参数边界、随机种子、数据划分、代码／资产哈希、优化状态、每次录制的损失和最优参数。
- [ ] 检查多随机种子与重复录制、参数敏感性、卡边界和参数稳定性。
  轨迹误差下降只证明该条件下拟合改善，不证明得到唯一真实物理值。
- [ ] 在独立验证集上比较末端位置／姿态、各关节位置／速度误差，以及换增益／姿态后的表现。
  根据实际任务预先确定门槛，上游约 3.6 mm 的结果不作为本机保证。
- [ ] 两台机械臂分别采集、拟合和验证；不能默认共享同一组摩擦参数。
- [ ] 导出有版本的 profile：机器人／关节名称、参数／单位、控制器契约、负载／手型、
  模型与源码版本、辨识 dt、延迟语义、数据哈希、验证范围／结果与限制。
  新 checkout 重放 profile 应在规定容差内复现指标；默认不修改 USD 和训练配置。

### Joint 轨迹方案（已选实验结构，幅度和频带尚待现场确定）

| 阶段 | 轨迹 | 用途 |
| --- | --- | --- |
| 日志验收 | 静止保持，再逐关节小幅低频正弦，其余关节保持 | 检查方向、目标滤波、记录与结束行为；不能据此完成全部参数辨识 |
| 辨识 A | J1 到 J7 逐关节平滑 chirp，覆盖正反运动与多种速度 | 观察不同频率的响应、跟踪能力和摩擦影响；其他关节的响应也要记录 |
| 辨识 B | 多关节 multisine，各关节使用不同相位/频率组合，必要时先分组 | 补充同时运动与耦合信息，检查实际信号是否能约束待拟合参数 |
| 独立验证 | 预先保留的新相位 multisine 或不同扫频，另加任务范围内的平滑关节路点轨迹 | 检查新轨迹和实际动作分布，避免只复现拟合数据 |

- [ ] 在实际任务使用的几个已检查姿态附近设计轨迹；逐次改变姿态、频带或幅度，
  保存各次配置。首轮固定 XHand 手型、空手、自由空间，接触与手指动力学另行验证。
- [ ] 轨迹采用平滑包络和受限的起止过渡，不能直接跳变到正弦的首点。
  对单频成分，速度/加速度幅值分别随 `2*pi*f*A`、`(2*pi*f)^2*A` 增长；
  多频叠加、chirp 和包络段必须检查完整导数，不能只检查各个正弦分量。
- [ ] 在负载确认与低幅验收后，依据响应信噪比、跟踪误差、力矩及变化率逐步选定频带。
  高频提高时相应缩小幅度，但不能缩到实际关节几乎不动仍声称辨识了该频带。
  保留控制增益与滤波不变，另用独立实验检查换增益后的迁移。
- [ ] 针对接近零速的双向往返保留数据，并检查静摩擦、动摩擦、惯量和延迟的可辨识性。
  单次低频运动不保证能分离全部参数；检查重复拟合稳定性、参数边界和验证误差。
- [ ] 在调参前固定验证划分，以整次录制为单位分组；不能把同一段轨迹相邻采样点
  随机拆成训练和验证。逐关节初查之后，至少保留一组多关节独立验证。
- [ ] 区分训练集、调参验证集和最终测试集。若用某验证结果修改参数边界／模型／轨迹，
  它不再是未见数据，需要另留最终测试；最终误差阈值在测试前按任务需要和重复性确定。

设计参考：[DLR 关于闭环机器人 chirp 与 multisine 激励的研究](https://elib.dlr.de/78270/)。
上述 joint 实验顺序是面向本项目的建议，不是该论文或 FrankaTwin 提供的实机参数。

## 5. 策略部署前

- [ ] 固定通过验证的模型、工具配置与控制参数版本，保证训练／部署的观测和动作约定一致。
- [ ] 在当前训练的 120 Hz physics / 60 Hz policy 链路单独评估迁移。
  1 kHz 的 3 ms delay 不是 3 个 120 Hz 步长，也不是 3 个 policy step；实现并验证时间模型后再接入。
- [ ] 当前 sim 的 implicit PD 与真机显式 joint impedance 不是已验证等价实现。
  选择验证近似或另加可选匹配控制器，保存 baseline 对照；仅通过 profile 显式启用。
  保留 19 actions、110 policy obs、132 critic obs 及任务／训练默认设置；维度相同不保证旧 checkpoint 行为兼容。
- [ ] 手指驱动、接触材料、物体动力学与变化手型单独验证，不能由七关节 arm SI 代替。
- [ ] 若部署链包含 MuJoCo，明确它的角色；IsaacLab 与真机的对齐不能替代 MuJoCo 与真机的验证。
- [ ] 先在目标任务范围内验证单臂策略，再评估双臂执行同步、公共坐标、碰撞和故障处理。
  现有双臂 coordinator 不等于同步执行或硬件停止保障。

## 6. 交付顺序与最终完成定义

| 门槛 | 执行方 | 必需证据 |
| --- | --- | --- |
| G0：有效数据 | 真机侧 + sim 校验器 | writer 修复、完整逐周期 joint 目标／状态、实际配置和负载来源、无无法解释的时间／丢帧问题 |
| G1：可信回放 | sim | 控制器夹具、正确驱动／重力／滤波约定、可重复的单环境 nominal baseline |
| G2：可用优化 | sim | 参数生效读回与行为检查、小规模搜索、失败处理、断点／profile 复现 |
| G3：有效 profile | sim + 独立真机数据 | 未见录制优于或满足预定 baseline 门槛、重复性与可辨识性报告、导出与干净环境复现 |
| G4：训练迁移 | sim／部署评估 | 实际控制器和步长下可选启用的对照实验，明确可用范围和未解决误差 |

下一项具体工作：在 NUC 构建并验收已修复的 writer 和 joint 日志，补实验元数据与文件导出；
sim 仓库可独立先做 schema、合成夹具与校验器，再做单环境 controller replay。
G0 和 G1 通过后再开始正式拟合。本地文档完整不代表任何硬件门槛或 SI 实现已通过。
